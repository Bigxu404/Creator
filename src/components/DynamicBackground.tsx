"use client";

export function DynamicBackground() {
  return (
    <div className="fixed inset-0 z-[-1] bg-[#0a0a0a]">
      {/* 
        3D 场景占位符。
        当你完成建模并导出 Spline 或 GLTF 模型后，我们将在这里挂载你的 3D 资产。
      */}
    </div>
  );
}
